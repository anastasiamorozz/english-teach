import { observer } from 'mobx-react-lite';
import React, { FC } from 'react';

const TopicsPage: FC = () => {
    return (
        <div>
            Topics
        </div>
    );
}

export default observer(TopicsPage);