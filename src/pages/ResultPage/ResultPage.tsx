import { observer } from 'mobx-react-lite';
import React, { FC } from 'react';

const ResultPage:FC = () => {
    return (
        <div>
            result
        </div>
    );
}

export default observer(ResultPage);